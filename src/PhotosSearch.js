import React from 'react';

export const PhotosSearch = () => (
  <div id="photosSearch">
    <input
      type="search"
      placeholder="Search free high-resolution photos"
    ></input>
  </div>
);